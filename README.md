# Telegram Бот для Яндекс Календаря

Голосовой бот для создания событий в Яндекс Календаре через Telegram. Просто отправьте голосовое сообщение с описанием события, и бот автоматически добавит его в календарь с напоминаниями.

## 🚀 Возможности

- 🎤 **Голосовой ввод** - отправляйте голосовые сообщения с описанием событий
- 🔤 **Распознавание речи** - автоматическая транскрибация через Yandex SpeechKit
- ✂️ **Обработка больших файлов** - автоматическое разделение голосовых сообщений больше 1 МБ на части
- 🤖 **Умная обработка** - извлечение даты, времени и названия события через Google Gemini 1.5 Flash
- 📅 **Интеграция с Яндекс Календарем** - автоматическое создание событий через CalDAV
- 🔔 **Уведомления** - напоминания за 60 и 15 минут до события
- 📋 **Список событий** - просмотр ближайших событий через команду /list

## 📋 Требования

- Python 3.10+
- **ffmpeg** (для обработки больших голосовых сообщений) - установить можно [здесь](https://ffmpeg.org/download.html)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- Yandex SpeechKit API Key и Folder ID (для транскрибации голоса - получить [здесь](https://cloud.yandex.ru/docs/speechkit/))
- Google Gemini API Key (для обработки текста - получить [здесь](https://makersuite.google.com/app/apikey))
- Яндекс аккаунт с настроенным паролем приложения

### Установка ffmpeg

**Что такое ffmpeg?**
ffmpeg - это **консольная утилита** (командная программа), а не Python библиотека. Она устанавливается отдельно в операционную систему и используется для работы с аудио и видео файлами.

**Установка:**

**Linux:**
```bash
sudo apt-get update
sudo apt-get install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

**Windows:**
Скачайте с [официального сайта](https://ffmpeg.org/download.html) и добавьте в PATH.

**На Railway/Heroku/других платформах:**
Добавьте ffmpeg в зависимости вашего проекта. Для Railway можно добавить в `railway.json` или использовать buildpack.

**Примечание:** 
- Если ffmpeg не установлен, бот все равно будет работать, но не сможет обрабатывать голосовые сообщения больше 1 МБ
- В этом случае пользователь получит понятное сообщение с просьбой записать более короткое сообщение
- Альтернативы (pydub и другие Python библиотеки) все равно требуют ffmpeg под капотом

## 🛠 Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd calendar-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# OpenAI API Key (для транскрибации голоса через Whisper)
# Получить можно здесь: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini API Key (для обработки текста)
# Получить можно здесь: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Яндекс Календарь
YANDEX_USER=your_email@yandex.ru
YANDEX_PASS=your_app_password_here

# Настройки уведомлений (в минутах до события)
NOTIFICATION_TIMES=15,60

# Часовой пояс
TIMEZONE=Europe/Moscow

# База данных (SQLite)
DATABASE_URL=sqlite+aiosqlite:///./calendar_bot.db
```

### 4. Настройка Яндекс Календаря

1. Зайдите в [Яндекс ID](https://id.yandex.ru/)
2. Перейдите в раздел "Безопасность"
3. Найдите "Пароли приложений"
4. Создайте новый пароль для "Календаря"
5. Скопируйте созданный пароль в переменную `YANDEX_PASS` в `.env`

⚠️ **Важно:** Используйте именно пароль приложения, а не пароль от почты!

## 🚀 Запуск

### Локальный запуск

```bash
python main.py
```

### Запуск на Railway

1. Создайте новый проект на [Railway](https://railway.app)
2. Подключите ваш репозиторий
3. Добавьте все переменные окружения из `.env` в настройках проекта Railway
4. Railway автоматически определит Python проект и запустит его

Railway автоматически использует `railway.json` для конфигурации.

## 📱 Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/list` - Показать ближайшие события

### Примеры голосовых сообщений

- "Поставь встречу с клиентом на завтра в 15:00"
- "Созвон с командой послезавтра в 10 утра на час"
- "Напомни про презентацию через 2 дня в 14:30"
- "Встреча с директором сегодня в 18:00"

Бот автоматически:
1. Распознает речь
2. Извлечет название события, дату и время
3. Создаст событие в Яндекс Календаре
4. Настроит напоминания

## 🏗 Архитектура

```
┌─────────────┐
│   Telegram  │
│     Bot    │
└──────┬──────┘
       │
       ▼
┌─────────────┐     ┌──────────────┐
│ Yandex      │────▶│  Транскрипция│
│ SpeechKit   │     │    (STT)     │
└─────────────┘     └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ Gemini 1.5   │
                    │    Flash     │
                    │     (NLU)    │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ Яндекс Календ.│
                    │   (CalDAV)    │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │   База данных │
                    │   (SQLite)    │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │ APScheduler  │
                    │ (Уведомления)│
                    └──────────────┘
```

## 📁 Структура проекта

```
.
├── main.py                 # Точка входа
├── bot.py                  # Telegram бот
├── config.py               # Конфигурация
├── transcription.py        # Транскрибация голоса
├── nlu_service.py          # Обработка естественного языка
├── calendar_service.py     # Работа с Яндекс Календарем
├── database.py             # Модели базы данных
├── scheduler.py            # Планировщик уведомлений
├── requirements.txt        # Зависимости
├── railway.json           # Конфигурация Railway
├── .env.example           # Пример переменных окружения
└── README.md              # Документация
```

## 🔧 Технический стек

- **aiogram 3.x** - Асинхронный Telegram бот
- **Yandex SpeechKit** - Распознавание речи
- **Google Gemini 1.5 Flash** - Обработка естественного языка и извлечение данных о событиях
- **CalDAV** - Протокол для работы с Яндекс Календарем
- **SQLAlchemy** - ORM для работы с базой данных
- **APScheduler** - Планировщик задач для уведомлений
- **SQLite** - База данных для хранения событий

## 🐛 Решение проблем

### Бот не отвечает

- Проверьте, что `TELEGRAM_BOT_TOKEN` указан правильно
- Убедитесь, что бот запущен и работает

### Ошибки при создании событий

- Проверьте правильность `YANDEX_USER` и `YANDEX_PASS`
- Убедитесь, что используете пароль приложения, а не пароль от почты
- Проверьте доступ к Яндекс Календарю

### Ошибки транскрибации

- Проверьте наличие `OPENAI_API_KEY`
- Убедитесь, что API ключ действителен (получить можно [здесь](https://platform.openai.com/api-keys))
- Проверьте, что на счету OpenAI есть средства
- Если получаете ошибку о большом размере файла, убедитесь, что установлен **ffmpeg** (см. раздел "Требования")
- Бот автоматически разделяет файлы больше 1 МБ на части, если установлен ffmpeg

### Ошибки обработки текста (Gemini)

- Проверьте наличие `GEMINI_API_KEY`
- Убедитесь, что API ключ действителен (получить можно [здесь](https://makersuite.google.com/app/apikey))
- ⚠️ **Важно:** Google API может блокировать запросы с российских IP. Если бот работает на российском сервере:
  - Используйте прокси для запросов к Gemini API
  - Или рассмотрите альтернативу: YandexGPT через Yandex Cloud (требует адаптации кода)

### События создаются в неправильное время

- Проверьте настройку `TIMEZONE` в `.env`
- Убедитесь, что часовой пояс указан правильно (например, `Europe/Moscow`)

## ⚡ Быстрый старт

1. **Создайте Telegram бота:**
   - Напишите [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте команду `/newbot`
   - Следуйте инструкциям и получите токен

2. **Получите Yandex SpeechKit API ключ и Folder ID:**
   - Зарегистрируйтесь на [Yandex Cloud](https://cloud.yandex.ru)
   - Создайте каталог (folder) в Yandex Cloud
   - Получите Folder ID: [инструкция](https://cloud.yandex.ru/docs/resource-manager/operations/folder/get-id)
   - Создайте API ключ: [инструкция](https://cloud.yandex.ru/iam/keys)
   - Включите SpeechKit API в вашем каталоге

3. **Получите Google Gemini API ключ:**
   - Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
   - Войдите в свой Google аккаунт
   - Создайте новый API ключ
   - Скопируйте ключ (он понадобится для `.env`)

4. **Настройте Яндекс Календарь:**
   - Зайдите в [Яндекс ID](https://id.yandex.ru/) → Безопасность → Пароли приложений
   - Создайте пароль для "Календаря"

5. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env и заполните все значения:
   # - TELEGRAM_BOT_TOKEN
   # - OPENAI_API_KEY
   # - GEMINI_API_KEY
   # - YANDEX_USER (опционально, можно настроить через /setup)
   # - YANDEX_PASS (опционально, можно настроить через /setup)
   ```

6. **Запустите бота:**
   ```bash
   pip install -r requirements.txt
   python main.py
   ```

7. **Протестируйте:**
   - Найдите вашего бота в Telegram
   - Отправьте команду `/start`
   - Отправьте голосовое сообщение: "Поставь встречу на завтра в 15:00"

## 📝 Лицензия

MIT License

## 🤝 Вклад

Приветствуются любые улучшения и предложения! Создавайте issues и pull requests.
